# React Native ♾️ INIFINITE Carousel with FlatList

#### basic

```js
let len = 10;
let currentIndex = 0;
do {
    let newIndex = (currentIndex + 1) % len;
    currentIndex = newIndex
} while(true)
```